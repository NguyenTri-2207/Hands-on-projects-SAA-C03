# Shared utilities — implement as needed across Lambdas

Planned modules:

- `dynamodb.py` — put_item, update_status, query_audit_steps
- `s3_utils.py` — read_json, write_json, parse_s3_event
- `errors.py` — custom error codes and structured error responses
- `constants.py` — status enums, severity thresholds

Implement during Phase 2–4 as code is written.
